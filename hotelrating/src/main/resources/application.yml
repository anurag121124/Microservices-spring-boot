server:
  port: 8083

spring:
  data:
    mongodb:
      uri: mongodb+srv://root:root@microservices.dseap6i.mongodb.net/?retryWrites=true&w=majority
      database: microservices
  application:
    name: RATING_SERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
